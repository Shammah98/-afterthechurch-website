import {isAdmin} from '@/lib/auth';import {getAdminClient} from '@/lib/supabase';
export const dynamic='force-dynamic';
export default async function Admin({searchParams}:{searchParams:Promise<{error?:string}>}){const ok=await isAdmin();const sp=await searchParams;if(!ok)return <section className="page-head"><div className="wrap"><h1>Moderator login</h1><form className="form-card" action="/api/admin/login" method="post"><div className="field"><label>Password</label><input type="password" name="password" required/></div>{sp.error&&<p className="notice warning">Incorrect password.</p>}<button className="button">Log in</button></form></div></section>;const {data=[]}=await getAdminClient().from('stories').select('*').order('created_at',{ascending:false});return <section className="page-head"><div className="wrap"><div className="inline"><h1>Moderation queue</h1><form action="/api/admin/logout" method="post"><button className="button secondary">Log out</button></form></div><table className="admin-table"><thead><tr><th>Status</th><th>Submission</th><th>Private details</th><th>Actions</th></tr></thead><tbody>{data?.map((s:any)=><tr key={s.id}><td>{s.status}</td><td><strong>{s.title}</strong><p>{s.summary}</p><details><summary>Full story</summary><p style={{whiteSpace:'pre-wrap'}}>{s.body}</p></details></td><td><p>{s.email}</p><p>{s.privacy_level}</p><p>Original church: {s.church_name||'—'}</p></td><td><div className="inline"><form action={`/api/admin/stories/${s.id}`} method="post"><button className="button small" name="action" value="approve">Approve</button></form><form action={`/api/admin/stories/${s.id}`} method="post"><button className="button secondary small" name="action" value="reject">Reject</button></form><form action={`/api/admin/stories/${s.id}`} method="post"><button className="button danger small" name="action" value="delete">Delete</button></form></div></td></tr>)}</tbody></table></div></section>}
